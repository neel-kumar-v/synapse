{
  "name": "@cloudflare/sandbox-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-starter-files.mjs",
    "dev": "npm run generate && wrangler dev --local",
    "dev:remote": "npm run generate && wrangler dev",
    "deploy": "npm run generate && wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "^0.7.8",
    "@types/node": "^24.10.15",
    "@types/react": "^19.2.14",
    "react": "^19.2.4",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  }
}
